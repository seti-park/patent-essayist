# Posting checklist — intel-us20260191095-backend-hbm

## Cover image
- **Candidate:** FIG. 1B (`fig-01B.png`), the isometric 8-high die stack on a base die — the
  claimed core (a memory die stack, "8-high and beyond," over a base die; claim 1 + `[0023]`)
  and the highest instant force in a feed card ("a new kind of tall memory chip").
- **Crop guidance:** the source sheet was authored landscape and rotated upright in Phase 0
  (the USPTO boilerplate header runs sideways; diagram + "FIG. 1B" caption are upright). For a
  5:2 feed cover, crop to the die-stack body (the tower + base die + the aligned DB/TSV columns),
  excluding the sideways header band. Auto-crop intentionally NOT shipped — crop at publish time.
- **Fallback:** FIG. 1A (`fig-01A.png`, logic die beside the memory stack — the thesis-in-one-image)
  as header, FIG. 1B demoted to body, if the crop fails. (1A carries 7 numerals, 100-106, over
  the ≤6 cover-caption budget, so it is the fallback, not the default.)
- Cover-caption numeral count 6 ("110"-"115"), within the ≤6 feed budget (SURF-003).

## Figures placed (in-body)
- FIG. 1B — header/cover (the claimed die stack)
- FIG. 1F — §2 (the back-end "TRANSISTOR" layers + exploded inset 150A — the claimed cell)
- FIG. 1G — §3 (basic building block 160: sub-channels 161A-161H + TSV gutters 162-165)
- FIG. 1A — §4 (logic die 106 beside HBM stack 104 on one interposer — the co-package thesis image)
- NOT rendered (prose pointers instead): FIG. 1C, 1D, 1E, 1H, 2, 3, 4, 5, 6, 7.

## Pre-post verification
- Confirm the verdict stays **measured, both directions**: it leads with the direction-commitment
  and keeps the two this-filing anti-hype guards (1T1C capacitor ceiling; back-end-cell has no
  public proof point yet) — no safe-harbor boilerplate, no overreach (`gate_hedge` PASS).
- Confirm §4 makes **no claim about the filing's own bonding method** (the false "instead of
  hybrid bonding" dichotomy was corrected; `[0020]` both-sided HBI / `[0023]` wafer-to-wafer stay
  uncontradicted), and that the ZAM contrast rests on the back-end-cell axis, not the interconnect.
- Confirm the "who can make HBM" strategic payload reads as the essay's **labeled inference**, not
  as a patent claim; and that no ZAM spec (Z-angle, hybrid bonding, ~171 mm², 2× HBM4, Powerchip)
  is attributed to the filing.
- Confirm "1T1C" is never called capacitor-less, and the channel material (IGZO/oxide) is never
  attributed to the patent.
- publication.md is the strip source (one line per paragraph); Sources block intact; em-dash-free.
