<!--
  handoff/02-compose/thesis-trace.md
  Produced by: essay-en-composer (Phase 2 Compose, Step 7) — draft_version 3
  (round-1 revision: spine → section mapping unchanged; word_actual recounts, two anchor
  additions ([0178] §4, [0259] §5), and presentation notes updated per
  revision-response.round-1.md. Section list and figure placements did not move.)
  (selfaudit-round-1 revision, origin self-post-accept: spine → section mapping still
  unchanged; anchor-precision fixes ([0136]/[0337]/[0384]/[0418]/[0125]/[0254]/[0259]/
  [0112]/[0032] additions or swaps), one labeled-analysis steelman-completion sentence
  added to 6-boundary (sa1B-F1, within the section's existing concede-then-refine beat),
  and clause-level narrowing in 7-verdict (sa1B-F2). Section list, figure placements,
  and the closing directive did not move. Delta log: handoff/03-edit/revision-notes.md.)
  (selfaudit-round-2 revision, origin self-post-accept, draft_version 4: spine → section
  mapping unchanged; anchor completions in 3-wiring ([0037]/[0039] per sa2G-F1, [0130]
  per sa2G-F2); the 6-boundary steelman's "In practice" sentence expanded into a
  five-sentence three-leg design-around inventory paragraph (switch-instead /
  superset-add / firmware-decline; sa2B-F1 + sa2G-F3, orchestrator-arbitrated as one
  claim-scope precision edit); 4-traffic [0061] decimals demoted to labeled
  approximations with a one-clause p/q gloss (sa2A-F1); 2-grant claims characterization
  narrowed to "a cluster" (sa2A-F5); 6-boundary memory-absence one-liner cut (sa2A-F3,
  thesis echo now <=3). §7 byte-identical to draft_version 3. word_actual re-measured
  for ALL sections this round as whitespace-token counts on publication.md section
  bodies (previous values were stale from round 1). Delta log:
  handoff/03-edit/revision-notes.md.)
  (selfaudit-round-3 revision, origin self-post-accept, draft_version 5 — FINAL round,
  self-audit cap reached: spine → section mapping unchanged; 6-boundary design-around
  inventory reworked so firmware-avoidability attaches to claim 18's routing rule (the
  same kind of limitation claims 8/9/11 stack on top of claim 1's structure, per the
  granted dependency chain) and the decline is priced (rerouting forfeits the cheap
  disjoint-family overlap) — sa3B-F1 + folded sa3B-F2; the switch-out sentence gained
  the patent's-own-dichotomy grounding [0026], [0032] (sa3G low-confidence row 2); LVI
  absence narrowed to filing-only scope (sa3G-F1); 5-workload QKV glossed at first use
  (sa3A-F1); 3-wiring [0026] quote subject corrected to "a data switch" (sa3A-F3);
  6-boundary "Described, never claimed." echo cut, verdict echo back to <=3 sections
  (sa3A-F5 = sa3B-F6). §7 byte-identical to draft_version 4 (cmp on §7-to-EOF).
  word_actual re-measured for all sections; §1/§2/§4/§7 unchanged. Delta log +
  closing status: handoff/03-edit/revision-notes.md.)
  Externalizes the composer's Step-3 section plan so Phase 3 Edit Pass 4 can check
  thesis-section alignment against thesis-spine.md's spine → section trace.
  word_actual measured on publication.md per section (includes captions + blockquotes).
-->

# Thesis Trace

## Spine source

- **Spine**: handoff/01-design/thesis-spine.md (locked; executed without deviation)
- **One-line spine**: Etched's "Tensor parallel group" patent substantiates the interconnect
  half of its Cluster-Scale Memory story, a switchless topology in which every chip reaches
  every other chip through at most one intermediate chip, and conspicuously not the
  memory-pool half.
- **Q7 hook**: corporate-narrative-friction (Etched stealth-exit thread 2026-07 vs
  US 12,361,091 B1 granted 2025-07-15)
- **closing_posture**: firm (copied into draft frontmatter; verdict drafted first per the
  closing directive: call leads, one THIS-patent anti-hype guard, closing-binary-test)

## Section → spine mapping

### 1-lead — "The Memory Story Has a Checkable Half"
- **Spine element carried**: Q7 hook (thread's CSM claims vs what the 2025 grant locks); BLUF
  verdict stated declaratively in paragraph 1
- **voice_canon_reference**: `opening-corporate-event-spacex-xai-merger-exclusion`
- **paragraph_anchors_used**: `[0386]`, `[0387]` (paraphrase only; no claim quotes spent in
  the lead, per phase2-handoff-notes citation priority)
- **external_facts_used**: `etched-csm-thread-2026-07`, `etched-lvi-biz-thread-2026-07`,
  `etched-family-trio-wips` (grant date/title identity)
- **word_target / word_actual**: 230 / 246 (round-1 revision: ¶1 recast in
  halves-of-the-CSM-claim vocabulary, "pillar" reserved for LVI/CSM per r1-F1; ¶4
  closing restatement cut per r1-F7)

### 2-grant — "A Structure Patent Etched Keeps Paying to Extend"
- **Spine element carried**: document identity; same-day trio division of labor (903 method /
  091 wiring / 262 umbrella); sole inventor; exam period presented as "inside nine
  months" (266-day computation carried in the derived-counts footnote, r1-F12); PCT +
  continuation commitment signal with the abroad/at-home gloss split (r1-F11); siblings
  capped at one paragraph
- **voice_canon_reference**: `closing-aphoristic-landing-corporate-story-vs-patents` (contrast
  cadence for the trio/commitment beat)
- **paragraph_anchors_used**: `[0336]` (Examples-family mapping, patent-internal corroboration)
- **external_facts_used**: `etched-family-trio-wips`
- **word_target / word_actual**: 230 / 197 (now two paragraphs: trio identity |
  commitment signal, split per r1-F10; "an AI cluster" narrowed to "a cluster" in the
  every-claim-one-idea characterization per sa2A-F5 — claims are silent on AI, as §5
  itself states)

### 3-wiring — "Every Chip Reaches Every Chip Without a Switch"
- **Spine element carried**: Axis 1 (claim 1 core, q-0386-1 blockquote + q-0387-1 inline) +
  Axis 2 (`[0026]` switch cost) + Axis 4 engineering baseline (switch `[0032]` / full mesh
  `[0130]` foils) + Axis 3 effect (`[0124]`); secondary technical-impossibility hook resolved
  in-body ("a switch or a wire per pair; claim 1 delivers the reach with neither")
- **voice_canon_reference**: `opening-industry-norm-reversal-xray-tesla` (norm + cost, then
  one-sentence reversal)
- **paragraph_anchors_used**: `[0021]`, `[0036]`, `[0037]`, `[0039]`, `[0032]`, `[0026]`,
  `[0130]`, `[0126]`, `[0386]`, `[0128]`, `[0387]`, `[0143]`, `[0124]` (`[0037]`/`[0039]`
  added per sa2G-F1: the reduction/gather definitional content sits there, not at
  `[0036]` alone; `[0130]` added on the two-hop clause per sa2G-F2)
- **external_facts_used**: [] (16-vs-28 link count presented as arithmetic read off the
  figure, flagged as such in body + footnote)
- **word_target / word_actual**: 420 / 521 (includes claim-1 blockquote + FIG. 1 caption +
  K4,4 parenthetical; [0026] quote subject corrected to "a data switch", the term
  [0026] itself uses, per sa3A-F3 — [0032] keeps the networking-switch clause)

### 4-traffic — "The Wiring Schedules the Traffic"
- **Spine element carried**: claims 8/9/11/18 channel-family segregation + simultaneity;
  balanced links `[0168]`; the `[0061]` split-factor optimum spent once (the one
  nerd-candy number, now presented as labeled approximations "about 4.35 and about 1.84"
  with a one-clause gloss of what p and q set, per sa2A-F1; divisor rounding 4/2 and the
  input-on-four-of-eight reading unchanged); gather-on-730/reduction-on-740 labeled as
  the description's example, not claim-locked
- **voice_canon_reference**: `inline-bold-thesis-anchor-etherloop-steady-state` (mechanism
  cadence: property stated, cost/consequence absorbed; no bold used in this section)
- **paragraph_anchors_used**: `[0135]`, `[0138]`, `[0139]`, `[0123]`, `[0140]`, `[0142]`,
  `[0168]`, `[0178]`, `[0061]` (`[0178]` added per r1-F15: first-family mirror of the
  balanced-channel statement)
- **external_facts_used**: []
- **word_target / word_actual**: 330 / 451 (includes q-0140-2 blockquote + FIG. 7A/7B captions
  + decomposition pointer paragraph; closing beat now splits wired-in channels from
  split-supplied traffic per r1-F5)

### 5-workload — "The Description Aims It at Transformer Decoding"
- **Spine element carried**: Axis 3 payoff at workload level; description-level labeling
  throughout ("None of this is claimed for AI" opens the section); MLP 4x pressure
  (`[0313]`, `[0258]`, `[0121]`); never-gather invariant (q-0278-1 blockquote, `[0267]`) as
  the honest kernel of the memory idea, closed with "It does not claim the store"
- **voice_canon_reference**: `inline-bold-thesis-anchor-photon-in-8bit-optimization`
  (gloss-then-meaning rhythm for MLP/4x; no bold used in this section)
- **paragraph_anchors_used**: `[0251]`, `[0313]`, `[0252]`, `[0259]`, `[0258]`, `[0121]`,
  `[0278]`, `[0267]` (`[0259]` added per r1-F14: decoding block 930 sits outside the 905
  loop, now stated as a handoff)
- **external_facts_used**: []
- **word_target / word_actual**: 300 / 401 (includes q-0278-1 blockquote + FIG. 9A caption;
  QKV/Q-K-V glossed at first use per sa3A-F1, reader-profile rule 1 — the patent's
  sub-block names from [0254] kept verbatim so the FIG. 9A box mapping still holds)

### 6-boundary — "No Latency Number, No Memory Claim"
- **Spine element carried**: steelman beat, concede-then-refine (standard links `[0134]`,
  separate dies `[0133]`, no latency/bandwidth-magnitude limitation, cost-vs-latency
  sharpener), completed by a three-leg design-around inventory paragraph under the
  "In practice" label (switch-instead-of-direct-channels outside claim 1, grounded on
  the patent's own switch-vs-fixed-topology dichotomy `[0026]`, `[0032]` — sa3G
  low-confidence row 2; copy-and-add superset fabric arguably outside claims 1/14 as
  written, grounded on the quoted negative limitation `[0386]`; claim 18 tolerating the
  added links but binding the traffic, the same kind of rule claims 8/9/11 stack on top
  of claim 1's structure, with the firmware decline priced — rerouting forfeits the
  cheap disjoint-family overlap — sa2B-F1 + sa2G-F3, reworked per sa3B-F1 + folded
  sa3B-F2) + the essay's ONE bold thesis anchor at the turning point + memory 630
  described-never-claimed (`[0119]`, FIG. 6; the "no equivalent fence" one-liner cut per
  sa2A-F3 and the "Described, never claimed." assertion-form echo cut per sa3A-F5 =
  sa3B-F6, returning the verdict echo to <=3 sections — the paragraph keeps the point
  in evidence form via "no claim picks that option up" `[0133]` and the FIG. 6 caption)
  + claim-scope narrowing exhibit (`[0385]` "two or more" as the
  labeled before-picture vs claims 1/14 "four or more"; claim 18's floor-for-structure
  trade via FIGS. 7A/7B) + the single anti-hype guard (LVI + HBM/SRAM absence narrowed
  to filing-only scope per sa3G-F1 — the siblings' full text is not in this run's
  evidence; 18-month window noted exactly once)
- **voice_canon_reference**: `inline-bold-thesis-anchor-etherloop-steady-state` (reframing
  bold anchor realized HERE, the essay's only body bold)
- **paragraph_anchors_used**: `[0134]`, `[0133]`, `[0130]`, `[0124]`, `[0026]`, `[0032]`,
  `[0386]`, `[0387]`, `[0168]`, `[0061]`, `[0112]`, `[0119]`, `[0385]` (`[0032]`/`[0112]`
  present since selfaudit-1; `[0026]` added selfaudit-3 on the switch-dichotomy clause,
  second and final use of that anchor — first is §3's quoted cost sentence)
- **external_facts_used**: `etched-lvi-biz-thread-2026-07` (voltage claim, attributed),
  `etched-csm-thread-2026-07` (HBM/SRAM + "proprietary ..." quote)
- **word_target / word_actual**: 460 / 686 (includes FIG. 6 caption + standalone bold beat;
  memory-absence beat now absorbs `[0133]`'s optional memory devices as claims-level
  contrast per r1-F2; hop bound carried once per r1-F8; anti-hype-guard paragraph split
  per r1-F10; growth over target is finding-driven — the round-1 + selfaudit-1/2/3
  steelman completions all landed here, selfaudit-3 adding the priced firmware-decline
  sentence while cutting the echo and the sibling clause)

### 7-verdict — "One Leg Substantiated, One Leg Absent"
- **Spine element carried**: firm two-sided call leading the section; recap anchors only
  (`[0124]` second and final use, allowed once by the citation priority map);
  closing-binary-test falsifier (company-claimed summer-2026 racks make the topology
  inspectable); aphoristic contrast landing; no open-question close, no safe-harbor
  boilerplate, limits referenced ("the boundaries set out above") and not re-listed
- **voice_canon_reference**: `closing-binary-test-spacex-tesla-ecosystem-realized`,
  `closing-aphoristic-landing-corporate-story-vs-patents`
- **paragraph_anchors_used**: `[0387]`, `[0142]`, `[0168]`, `[0124]`, `[0386]`
- **external_facts_used**: `etched-lvi-biz-thread-2026-07` (ship date = binary test)
- **word_target / word_actual**: 220 / 196 (closing binary test recast as match/no-match
  against claim 1 per r1-F6; call, boundaries reference, and landing unchanged;
  byte-identical across selfaudit-2, draft_version 3 → 4)

## Coverage check

- All 4 axes carried: Axis 1 → §3 (claim 1) reinforced §4 (claims 8/9/11/18) and §6 (claim
  scope map); Axis 2 → §3 (`[0026]`) + §5 (`[0313]`); Axis 3 → §3 (`[0124]`) + §4 (`[0168]`)
  + §7 recap; Axis 4 → §3 (patent-internal engineering baseline) + §1/§6/§7 (narrative
  baseline, company-claimed and attributed).
- Q7 hook (corporate-narrative-friction) opens §1; secondary technical-impossibility hook
  resolved inside §3 as planned; steelman beat lands in §6 at full strength, then refines;
  since selfaudit-2 the concession also prices the design-around inventory (switch-instead
  / superset-add / firmware-decline) before the bold refinement, with the verdict call
  unchanged; since selfaudit-3 the firmware-decline leg carries its price (rerouting
  forfeits the cheap disjoint-family overlap) and claims 8/9/11 are presented as routing
  rules stacked on claim 1's structure, not traffic-only claims — reconciling the
  inventory with the fence paragraph's "non-generic, inference-shaped" element.
- Claim-scope traps respected: claims 1/14 stated as four-or-more per set; claim 18 stated as
  two-or-more with the four-group dual-channel-family structure and WITHOUT simultaneity;
  `[0385]` "two or more" used only in §6 as the labeled narrowing exhibit; eight devices /
  4 channels / 16 links presented as the worked example and figure arithmetic, never as
  claim-1 requirements; link technologies (`[0134]`) presented as description options
  (steelman fuel), never upgraded to a proprietary PHY; bandwidth equality (claim 5)
  distinguished from magnitude.
- Every `[xxxx]` used exists in invention-summary.md Quotable spans / Quote anchor table /
  anchored sections (gate_anchors + gate_quotes: PASS). Verbatim quirks preserved inside
  quotes (" 730 .", " 905 ." spacing); `[0123]`'s "for case of illustration" typo paraphrased
  outside quotation marks.
- Every external fact used traces to a fact-check-log Fact ID and appears in # Sources
  (Patents x3, Official statements x1, subgrouped). All company-number uses carry
  the-company-says attribution; the thread quote chain is flagged in # Footnotes.
- Open-question defaults taken (phase2-handoff-notes §e): graph label offered once in
  parentheses (K4,4); "the best layer is no layer" quoted once in the lead with attribution;
  title pattern = declarative reversal; sibling part-2 teaser left implicit (one-sentence 903
  coverage, no explicit teaser).
- Verdict discipline: closing_posture: firm in frontmatter; call leads §7; exactly one
  patent-specific anti-hype guard (LVI/HBM-SRAM absence, §6, filing-only scope since
  selfaudit-3, 18-month window once); no generic patent truisms as steelman or close
  (gate_hedge: PASS).
