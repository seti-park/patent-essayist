# Phase 2 hand-off notes

- **Mode / posture**: walkthrough · measured (patent-heavy strategic essay; apply conservative-
  grade factual discipline even though the assessment posture is the default measured).
- **Spine**: locked single spine (thesis-spine.md). Hook = technical-impossibility. Moat is the
  throughline/implication, not the hook.
- **Two filings, one essay**: keep '885 (bit lines / column) and '143 (word lines / row)
  distinct; the symmetry is the central image. Do not call them "one patent."
- **Figures**: six locked, essay-local numbering (figure-selection.md + input/figures-source-map.md).
  Header = Figure 1. Body figures embedded image-plus-caption (this deliverable is read inline,
  not uploaded later). Cite each figure's source by patent shorthand + paragraph anchor; **never
  write a literal source "FIG. N"** (the anchor gate would read it as an essay-figure ref).
- **Anchors**: only `[dddd]` tokens present in invention-summary may appear in the draft.
- **External facts**: every market/corporate claim cites a fact-check-log Fact ID and lands in
  `# Sources` (subgrouped: Patents / Official statements / News & media). Fence each as "not in
  the filing."
- **Do not assert** the forward-citation chain or any quantified bandwidth-gain percentage.
- **Voice fences**: composer is voice-on. Closing = aphoristic landing ("X is Y. But Z is W.").
- **Sources plan**: ~9 entries across 3 categories → subgrouped (all-or-nothing).
